{
  "name": "@ripejs/core",
  "packageManager": "yarn@4.3.1",
  "type": "module",
  "scripts": {
    "build": "rm -rf dist/ && rollup -c",
    "start": "node dist/index.mjs"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "@tinyhttp/app": "^2.2.4",
    "@tinyhttp/logger": "^2.0.0"
  },
  "devDependencies": {
    "typescript": "^5.5.3"
  }
}
